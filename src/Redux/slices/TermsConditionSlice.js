import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    checkBoxState : "",
}

export const termConditionslice = createSlice({
    name:"TermCondition",
    initialState,
    reducers:{

    }
});

export const {} = termConditionslice.actions;
export default termConditionslice.reducer
